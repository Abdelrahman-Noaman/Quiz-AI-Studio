# Used by the Next.js database health route and Drizzle configuration.
# The current application does not persist quiz data in PostgreSQL yet.
DATABASE_URL=postgresql://postgres:password@localhost:5432/quizai
